@model Portal.Business.ViewModels.DirectDebitTransactionResponse

@{
    ViewData["Title"] = "Direct Debit Status";
}

<!---- Page Title & Date --->
<div class="row">
    <div class="element-wrapper col-md-12 pb-0">
        <h6 class="element-header">
            Direct Debit Status
            @{
                var now = DateTime.Now;
                var time = now.ToString("HH:mm tt");
                var date = now.ToString("MMM dd yyyy");
            }
            <span class="date-timestamp float-right">@date, @time</span>
        </h6>
    </div>
</div>

@if (Model.ArmDdStatusCode == "0")
{
    <div class="row">
        <div class="col-lg-12 col-sm-12">
            <div class="element-box">
                <h3>Your transaction was successful.</h3>
            </div>
        </div>
    </div>
}
else
{
    <div class="row">
        <div class="col-lg-12 col-sm-12">
            <div class="element-box">
                <h3w>Your transaction was unsuccessful.</h3w>
            </div>
        </div>
    </div>
}

<div class="row">
    <div class="col-lg-8 col-sm-12">
        <div class="element-box">
            @if (Model != null)
            {
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr></tr>
                        </thead>
                        <tbody>
                            @if (Model.ArmDdRef != null)
                            {
                                <tr>
                                    <td>Transaction Reference</td>
                                    <td>@Model.ArmDdRef</td>
                                </tr>
                            }
                            @if (Model.ArmDdAmt > 0)
                            {
                                <tr>
                                    <td>Amount</td>
                                    <td>₦@Model.ArmDdAmt.ToString("F2")</td>
                                </tr>
                            }
                            @if (Model.ArmCcType != null)
                            {
                                <tr>
                                    <td>Card Type</td>
                                    <td>@Model.ArmCcType</td>
                                </tr>
                            }
                            @if (Model.ArmCcMask != null)
                            {
                                <tr>
                                    <td>Card Detail</td>
                                    <td>@Model.ArmCcMask</td>
                                </tr>
                            }
                            <tr>
                                <td>Status</td>
                                <td>@Model.ArmDdStatusMsg</td>
                            </tr>
                        </tbody>
                    </table>
                </div>

            }
            else
            {
                <h3>No update available.</h3>
            }
        </div>
        <br/>
        <div>
            <h4>
                For information or queries on this transaction, kindly email
                <a href="mailto:info@arminvestmentmanagers.com"> info@arminvestmentmanagers.com</a>.
            </h4>
        </div>
        <div class="b-t mt-3 pt-3">
            <a class="btn btn-primary" asp-area="Client" asp-controller="Dashboard" asp-action="Index">Back to dashboard</a>
        </div>
    </div>

    <div class="col-lg-4 col-sm-12">

        <div class="element-box">
            <form>
                <h5 class="element-box-header">
                    Ad Banner
                </h5>
                <div class="form-desc">

                </div>

            </form>
        </div>
    </div>
</div>